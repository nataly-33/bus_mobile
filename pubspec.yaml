name: buses_sig
description: "Sistema SIG de Microbuses - Santa Cruz de la Sierra"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.0
  http: ^1.2.0
  geolocator: ^12.0.0
  image_picker: ^1.0.0
  intl: ^0.19.0
  # arcgis_maps: ^200.6.0  # Descomentar tras setup nativo — ver GUIA_ARCGIS.md

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
